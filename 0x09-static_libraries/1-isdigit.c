#include "main.h"

/**
 * _isdigit - checks if a character is a digit
 *
 * @c: character to check
 *
 * Return: 1 if the character is a digit, else return 0
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
